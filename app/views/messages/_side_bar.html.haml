.side_bar
  .side_bar__header
    .side_bar__header__waku
      %h3.side_bar__header__waku__name
        = current_user.name
      %ul.side_bar__header__waku__icon
        %li.side_bar__header__waku__icon__icon-type-edit
        = link_to new_group_path do
          = icon('fas','edit', class: 'icon')
        %li.side_bar__header__waku__icon__icon-type-cog
        = link_to edit_user_path(current_user) do
          = icon('fas','cog', class: 'icon')
  .side_bar__group-list
    .side_bar__group-list__list
      - current_user.groups.each do |group|
        .side_bar__group-list__list__waku
          = link_to group_messages_path(group) do
            .side_bar__group-list__list__waku__group
              = group.name
            .side_bar__group-list__list__waku__message
              メッセージはまだありません。
